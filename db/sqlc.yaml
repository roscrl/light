version: "2"
sql:
  - engine: "sqlite"
    schema: "schema.sql"
    queries: "query.sql"
    database:
      uri: file:app.db
    rules:
      - sqlc/db-prepare
    strict_order_by: false
    gen:
      go:
        package: "sqlc"
        out: "sqlc"